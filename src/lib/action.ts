"use server"
import User from "./models";
import bcrypt from "bcryptjs";

export const registerSubmit = async () => {
    try {
        console.log('tout est bon man');
    } catch (err)  {
        console.log(err);
    }
}